@model List<User>
@{
    ViewData["Title"] = "Dashboard";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="admin-dashboard">
    <!-- Header -->
    <div class="dashboard-header">
        <div>
            <h2>
                <i class="fas fa-tachometer-alt me-2"></i>GUEST DASHBOARD
            </h2>
            <p>Welcome, @User.Identity.Name!</p>
        </div>
    </div>

   